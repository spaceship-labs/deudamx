'use strict';

describe('Directive: fullScreenWrapper', function () {

  // load the directive's module
  beforeEach(module('deudamxApp'));

  /*
  var element,
    scope;
  */
  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
